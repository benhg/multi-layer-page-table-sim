/**
 * File with test functions for simple mapping test
 */

#ifndef SIMPLE_MAPPING_H
#define SIMPLE_MAPPING_H

/**
 * Run the test(s). Return -1 if fails, 0 if passes
 */
int run_simple_mapping_test(ptw_sim_context_t *ctx);


#endif